{
  "url": "/",
  "method": "GET",
  "qs": {
    "engine": "{{parameters.engine}}",
    "{{...}}": "{{toCollection(ifempty(parameters.parameters, emptyarray), 'key', 'value')}}"
  },
  "response": {
    "output": "{{body}}"
  }
}